[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gp800-tool"
version = "0.1.0"
description = "Gilera GP800 ECU map analysis and safety validation tool"
requires-python = ">=3.10"
dependencies = [
    "click>=8.0",
]

[project.optional-dependencies]
kline = ["pyserial>=3.5"]
viz = ["matplotlib>=3.5"]
api = ["flask>=2.0", "python-multipart>=0.0.9", "pyserial>=3.5"]
api-full = ["flask>=2.0", "fastapi>=0.110", "uvicorn[standard]>=0.29", "python-multipart>=0.0.9", "pyserial>=3.5"]
dev = ["pytest>=7.0", "pyserial>=3.5"]

[project.scripts]
gp800-tool = "gp800_tool.cli:main"

[project.gui-scripts]
gp800-gui = "gp800_tool.gui:main"

[tool.setuptools.packages.find]
where = ["src"]
